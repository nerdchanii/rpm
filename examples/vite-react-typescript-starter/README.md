# Vite React TypeScript Starter Example

This directory preserves the sample Node project state that used to live in the repository root.

Use it when you need a small project for manual RPM install, add, or run checks without mutating the RPM source tree.
