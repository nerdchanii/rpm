# Semver Benchmarks

The semver benchmark operating document now lives at
`benches/BENCHMARKS.md`.

Keep benchmark runners, generated benchmark history, and human benchmark
checkpoint templates under `benches/` so benchmark behavior and artifacts stay
near the benchmark code.
