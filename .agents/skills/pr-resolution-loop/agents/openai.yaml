interface:
  display_name: "PR Resolution Loop"
  short_description: "Resolve PR feedback and hand off to the merge gate."
  default_prompt: "Use $pr-resolution-loop to resolve accepted PR feedback, validate it, and transition the issue to awaiting-merge."

policy:
  allow_implicit_invocation: false
