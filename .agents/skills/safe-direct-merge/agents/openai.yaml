interface:
  display_name: "Safe Direct Merge"
  short_description: "Explicitly authorized merge outside the lifecycle."
  default_prompt: "Use $safe-direct-merge only with explicit authorization to gate-check an out-of-lifecycle PR without deleting held worktrees."

policy:
  allow_implicit_invocation: false
