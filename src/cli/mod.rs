//! Command-line interface boundary.
//!
//! This module is the target home for argument parsing, command dispatch,
//! terminal presentation, and exit-code mapping. Remaining CLI-owned modules
//! stay in their legacy paths until later mechanical moves.

pub mod opt;
